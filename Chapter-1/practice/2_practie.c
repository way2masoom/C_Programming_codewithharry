//calculate the area of the circle and 
//modify the same program to claculate the volume of a cylinder given it's radius and height
#include<stdio.h>

    int main(){
//calculate the area of the circle
    int radius=3;
    float pi=3.14;

    printf("The are of the circle is = %f \n",pi*radius*radius);

//modify the same program to claculate the volume of a cylinder given it's radius and height
    int height=4;
    printf("The volume of the Cylinder is = %f \n",pi*radius*radius*height);


    return 0;
}